import test from"node:test";import assert from"node:assert/strict";import{readFile}from"node:fs/promises";const r=p=>readFile(new URL("../"+p,import.meta.url),"utf8");test("no collection APIs or unsafe CSP",async()=>{const v=await r("vercel.json");assert.doesNotMatch(v,/unsafe-inline/);const x=JSON.parse(await r("public/verification.json"));assert.deepEqual(x.dataCollectionEndpoints,[])});test("public boundary and current date",async()=>{const h=await r("public/index.html");assert.match(h,/not operating as a law firm/);assert.match(h,/20 August 2026/)});test("three versioned downloads",async()=>{for(const p of ["research-memo-workflow.md","case-chronology-workflow.md","contract-review-issue-log.md"])assert.match(await r("public/downloads/"+p),/Version 1.1/)})